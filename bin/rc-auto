#!/usr/bin/env node
const program = require('commander')
program.version(require('../package').version, '-v', '--version')
  .command('run', 'watch components')
  .command('refresh', 'refresh components...')
program.parse(process.argv)
